name: Batch

on:
  schedule:
    - cron: '25 14 * * *'
jobs:
  run:
    name: Scheduled action
    runs-on: ubuntu-latest
    steps:
      - name: run bf bot
        run: python ./cryptobot/bf_bot.py
